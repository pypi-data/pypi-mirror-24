===========
Netflix Packager for ContentHub
===========

Netflix Packager (npack) is used to package large file sequences for uploads to Netflix's ContentHub.
Common usage looks as follows

     npack zip -s sourceFolder -t targetFolder -f sequenceFileName.zip


Commands Supported
=========

* Checksum

* Zip (includes checksum)