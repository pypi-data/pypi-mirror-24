============
Installation
============

At the command line::

    $ easy_install django-initial-field

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-initial-field
    $ pip install django-initial-field
